// Redis configuration
configurable string redisHost = "localhost";
configurable int redisPort = 6379;
configurable string redisPassword = "";
configurable int cacheTtl = 300; // Cache TTL in seconds (5 minutes)
